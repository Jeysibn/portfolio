target-version = "py311"

[lint]
# Keep the backend lint contract explicit instead of inheriting Ruff's evolving
# default rule set. These are the correctness-focused rules this repository has
# historically relied on in CI.
select = ["E4", "E7", "E9", "F"]
